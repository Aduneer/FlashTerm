# Build output
/FlashTerm
/build/
*.o
*.d

# Atomic-save leftovers from an interrupted write
*.tmp

# Personal decks. The default deck is study data, not source: it changes on
# every review. Starter decks live in examples/ and are imported, not edited.
/flashcards.txt
